# Запуск LLM через Google Colab

## Шаг 1: Откройте Colab Notebook

1. Откройте файл `notebooks/llm_server_colab.ipynb` в Google Colab:
   - Перейдите на [colab.research.google.com](https://colab.research.google.com/)
   - Нажмите `File` → `Upload notebook`
   - Загрузите файл `llm_server_colab.ipynb` из папки `notebooks/`

2. **Важно:** Включите GPU:
   - Меню `Runtime` → `Change runtime type`
   - Выберите `GPU` → `T4` (бесплатный)
   - Нажмите `Save`

## Шаг 2: Запустите все ячейки

1. Нажмите `Runtime` → `Run all` (или `Ctrl+F9`)
2. Подождите ~5 минут (загрузка модели)
3. В последней ячейке появится публичный URL вида:
   ```
   📡 Публичный URL: https://xxxx-xx-xx-xx-xx.ngrok.io
   ```
4. **СКОПИРУЙТЕ ЭТОТ URL** (он нужен на следующем шаге)

## Шаг 3: Подключите Colab к бэкенду

1. Откройте файл `services/rfsd_backend/.env`
2. Добавьте строку (замените URL на ваш из Colab):
   ```env
   LLM_ENDPOINT_URL=https://xxxx-xx-xx-xx-xx.ngrok.io
   ```
3. Можете удалить строку с `OPENAI_API_KEY` (она больше не нужна)

## Шаг 4: Перезапустите бэкенд

В Терминале 1:
```powershell
Ctrl+C
uvicorn app.main:app --reload --port 8000
```

Вы должны увидеть:
```
INFO:app.agent_orchestrator:Using LLM endpoint: https://xxxx.ngrok.io
```

## Шаг 5: Проверка через Telegram

Напишите боту:
```
Проанализируй финансовое состояние ИНН 7722514880 за 2020-2023 и дай выводы
```

Через 20-40 секунд (сбор данных + генерация ответа) бот вернёт аналитический текст.

## Важно

- **Colab сессия живёт ~12 часов**, после чего отключается.
- Если Colab отключился, просто перезапустите notebook (Шаг 2).
- ngrok URL изменится при каждом перезапуске — обновляйте его в `.env`.

## Альтернатива: постоянный URL (ngrok Pro)

Если хотите стабильный URL (не меняющийся), зарегистрируйтесь на [ngrok.com](https://ngrok.com) и используйте свой authtoken в ячейке 4:

```python
ngrok.set_auth_token("ваш_токен")
public_url = ngrok.connect(8000, bind_tls=True)
```

Это бесплатно, но требует регистрации.
